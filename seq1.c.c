#include <stdio.h>
#include <stdlib.h>

int main()
{
int lado,area;
printf("digite o valor de um dos lados: ");
scanf("%d",&lado);
area=(lado*lado);
printf("a area do quadrado e: %d",area);
return 0;
}
