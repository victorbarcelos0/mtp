#include <stdio.h>
#include <stdlib.h>
int main(){
int a;
printf("Entre com um numero inteiro \n");
scanf("%d", &a);
if ( a % 2 == 0)
if (a % 7||3 == 0)
printf("Par, e multiplo de 3 ou 7", a);
else
printf("Par", a);
else
if (a % 5 == 0)
printf("Impar,e multiplo de 5", a);
else
printf("Impar", a);
return 0;
}
